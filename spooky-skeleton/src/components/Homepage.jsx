import React from 'react'
import { Grid, Typography } from '@material-ui/core'

const Homepage = () => {
    return (
        <Grid container>
            <Typography variant="overline">Henlo World</Typography>
        </Grid>
    )
}

export default Homepage